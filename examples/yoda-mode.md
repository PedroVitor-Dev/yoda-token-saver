# Yoda Mode Example

## User request

Explain how to fix a Git push rejected error.

## Yoda Mode response

Remote ahead, it is.

Run:

```bash
git pull --rebase origin main
git push origin main
```

Conflicts appear? Fix, commit, push again.

## Token efficiency note

This response uses a light Yoda-inspired style while keeping the answer clear and useful.
